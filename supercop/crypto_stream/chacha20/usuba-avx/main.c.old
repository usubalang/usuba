#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "stream.c"


int main() {
  char msg[115] = "Ladies and Gentlemen of the class of '99: If I could offer you only one tip for the future, sunscreen would be it.";

  /*
  unsigned char key[32];
  for (int i = 0; i < 32; i++) key[i] = i;
  unsigned char nonce[8] = { 0, 0, 0, 0x4a, 0, 0, 0, 0 };
  */
  
  unsigned char key[32] = { 0 };
  unsigned char nonce[8] = { 0 };
  
  
  unsigned char out[256];
  crypto_stream(out,256,nonce,key);

  //crypto_stream_xor(out,msg,115,nonce,key);

  for (int i = 0; i < 256; i++)
    printf("%02x ",out[i]);
  printf("\n");
}
